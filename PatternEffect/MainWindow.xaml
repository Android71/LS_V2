<Window x:Class="PatternEffect.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cc="clr-namespace:LS_Designer_WPF.Controls;assembly=CustomControls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="MVVM Light Application"
        Width="300"
        Height="300"
        DataContext="{Binding EffectModel,
                              Source={StaticResource Locator}}"
        mc:Ignorable="d ignore">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Skins/MainSkin.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid x:Name="LayoutRoot">
        <cc:SliderItem x:Name="sliderItem"
                       Width="272"
                       Height="25"
                       Margin="10,117,0,0"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       SelectionEnd="8"
                       SelectionStart="3"
                       Variant="RangeLeftLimit"
                       Value="5" />

        <TextBlock x:Name="textBlock"
                   Margin="34,155,0,0"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Text="{Binding Value,
                                  ElementName=sliderItem}"
                   TextWrapping="Wrap" />
        <cc:MultiSlider SliderList="{Binding SliderItems}" Width="242"
                        Height="53"
                        Margin="34,20,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top" />
    </Grid>
</Window>
